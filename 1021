#include <stdio.h>
int main()
{
	int i;
	char s[1000];
	int a[10]={0};
	scanf("%s",s);
	for(i=0;i<strlen(s);i++)
	a[s[i]-'0']++;
	for(i=0;i<10;i++)
	{
		if(a[i]!=0)
		printf("%d:%d\n",1,a[1]);
	}
 } 
